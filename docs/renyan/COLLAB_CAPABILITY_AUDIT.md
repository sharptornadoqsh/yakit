# 睿眼协作能力审计

| 要求序号 | 当前能力 | 对应代码文件 | 使用的服务地址或 API | 是否依赖企业服务 | 是否可以直接复用 | 是否需要执行阶段 6 | 风险 |
| ---: | --- | --- | --- | --- | --- | --- | --- |
| 19 | 部分存在 | `app/renderer/src/main/src/components/layout/utils.ts`；`app/renderer/src/main/src/utils/login.tsx`；`app/main/handlers/assets.js`；`app/main/handlers/queryHTTPFlow.js`；`app/renderer/src/main/src/types/global.d.ts` | `GET /api/system/config`；引擎 RPC `UploadRiskToOnline`、`HTTPFlowsToOnline` | 是 | 否 | 是 | 现有链路可向企业服务上传项目关联的漏洞与 HTTP Flow，但未发现多客户端读取同一数据集、结果同步、冲突处理或权限隔离证据。 |
| 20 | 只有前端页面，没有可用后端 | `app/renderer/src/main/src/pages/loginOperationMenu/RoleAdminPage.tsx`；`app/renderer/src/main/src/services/fetch.ts`；`app/main/api/index.js`；`app/main/httpServer.js` | `GET/POST/DELETE /api/roles`；`GET /api/roles/detail`；`GET /api/plugin/type`；企业模式代码地址为 `https://vip.yaklang.com` | 是 | 否，仅可保留页面与接口契约 | 是 | 仓库内只有 HTTP 转发器，没有角色业务路由；角色表单仅配置插件类型与插件标识，不能证明服务端对管理员、测试人员及其他资源实施授权。 |
| 21 | 部分存在 | `app/renderer/src/main/src/pages/softwareSettings/ProjectManage.tsx`；`app/main/preload.js`；`app/renderer/src/main/src/components/layout/utils.ts` | 引擎 RPC `GetProjects`、`NewProject`、`UpdateProject`、`DeleteProject`、`SetCurrentProject` | 客户端项目管理不依赖；服务端项目组无法确认 | 部分，客户端项目与文件夹能力可复用 | 是 | `FolderId` 与 `ChildFolderId` 证明本地或引擎侧文件夹层级存在，但不能证明团队项目组、成员关系、多用户持久化和服务端授权。 |
| 24 | 不存在 | `app/renderer/src/main/src/routes/renyanMenu.ts`；`app/renderer/src/main/src/pages/home/Home.tsx`；`app/main/handlers/assets.js`；`app/main/handlers/queryHTTPFlow.js` | 仅发现上传 RPC `UploadRiskToOnline`、`HTTPFlowsToOnline`；未发现服务端总览查询 API | 是，现有上传链路属于企业能力 | 否 | 否，本期允许不交付 | 客户端测试总览默认关闭，团队数据上传不能替代服务端按受管客户端查询历史流量和漏洞结果的能力。 |
| 28 | 只有前端页面，没有可用后端 | `app/renderer/src/main/src/pages/loginOperationMenu/AccountAdminPage.tsx`；`app/renderer/src/main/src/pages/loginOperationMenu/RoleAdminPage.tsx`；`app/renderer/src/main/src/components/layout/FuncDomain.tsx`；`app/main/api/index.js` | `GET/POST/DELETE /api/urm`；`GET/POST /api/urm/edit`；`POST /api/urm/reset/pwd`；`/api/roles`；`/api/department` | 是 | 否，仅可保留页面、类型与接口契约 | 是 | 账号、组织、角色分配和菜单显示均有前端实现，但仓库内没有用户权限业务服务，也没有服务端拒绝越权请求的证据。 |
| 29 | 部分存在 | `app/renderer/src/main/src/pages/SetPassword.tsx`；`app/renderer/src/main/src/components/ConfigPrivateDomain/ConfigPrivateDomain.tsx`；`app/renderer/src/main/src/pages/loginOperationMenu/AccountAdminPage.tsx` | 前端复杂度正则；`POST /api/urm/up/userinfo`；`POST /api/urm`；`POST /api/urm/reset/pwd` | 前端校验不依赖；服务端强制策略依赖企业服务 | 部分，变更密码表单可保留 | 是 | 变更密码表单检查长度、大小写、数字和特殊字符，但正则缺少结尾锚点；新建与重置密码由外部服务生成并返回，未发现统一的服务端复杂度策略。 |
| 30 | 不存在 | `app/renderer/src/main/src/components/ConfigPrivateDomain/ConfigPrivateDomain.tsx`；`app/renderer/src/main/src/store/index.ts`；`app/main/state.js` | `setRemoteValue(getRemoteHttpSettingGV())`；`yakitCodec.run` 的 `base64` 与 `base64-decode` | 客户端冲突不依赖；远端数据库状态无法确认 | 否 | 是 | 私有域配置把用户密码以可逆 Base64 写入远端键值并可还原到表单，不能满足不可恢复存储要求；服务端数据库形态仍无源码证据。 |

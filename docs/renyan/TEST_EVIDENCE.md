# 睿眼产品化测试证据

## 一、环境

| 项目 | 值 |
| --- | --- |
| 操作系统 | Windows |
| Node.js | 22.13.0 |
| Yarn | 1.22.22 |
| Python | 3.12.13 |
| Pillow | 12.1.1 |
| 当前分支 | `qsh` |
| 远端 | `origin` 指向 `https://github.com/sharptornadoqsh/yakit.git` |

## 二、准备与资源生成

| 操作 | 结果 |
| --- | --- |
| 初始 Git 同步与分支检查 | 成功；`qsh`、干净工作区、正确 Fork |
| 根依赖安装 | 最终成功；期间修复 Yarn 缓存损坏与 Electron 默认下载地址拒绝问题 |
| 主渲染依赖 | 成功 |
| 启动渲染依赖 | 成功 |
| Windows 引擎与扩展载荷 | 依据仓库既有来源准备，保持为忽略文件，不纳入提交 |
| `yarn brand:generate` | 成功；生成三十七个文件，PNG、ICO、ICNS 与 SVG 自检通过 |

## 三、最终命令结果

| 命令或检查 | 结果 | 证据摘要 |
| --- | --- | --- |
| `git branch --show-current` | 成功 | 输出 `qsh` |
| `git diff --check` | 成功 | 源码复核阶段无空白错误；提交前再次执行 |
| `yarn i18n:check` | 成功 | 三种语言的所有键一致，最终耗时约三秒 |
| `yarn ci:eslint` | 成功 | 零错误；一千七百零三项既有警告，最终耗时约二百四十四秒 |
| `yarn ci:tsc` | 成功 | 无类型错误，最终耗时约一百五十七秒 |
| `yarn ci:media-size` | 成功 | 媒体体积检查通过 |
| `yarn test:vitest --run` | 成功 | 十六个测试文件、二百零二项用例通过，最终耗时约五十秒 |
| 定向产品与托盘测试 | 成功 | 两个测试文件、八项用例通过 |
| 主侧与构建脚本语法检查 | 成功 | 产品、关于、托盘、打包与构建脚本通过 `node --check` |
| `codegraph sync .` | 成功 | 索引同步，受影响测试清单与全量测试集合一致 |
| `yarn build-renders-test-enterprise-no-license` | 成功 | 退出码为零，总耗时约六百一十九秒 |

## 四、构建类别证据

免许可证构建环境解析结果为：

```json
{"platform":"enterprise","license":"false","edition":"企业版（免许可证）"}
```

主渲染压缩产物包含以下构建对象：

```text
buildSha:"452abcef315dc343e3855d2ac3fbca50acbf1b93"
edition:"\u4f01\u4e1a\u7248\uff08\u514d\u8bb8\u53ef\u8bc1\uff09"
```

## 五、运行时检查

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 企业版免许可证开发应用启动 | 成功 | 使用最终双渲染输出启动 Electron，主进程可见且响应正常 |
| 窗口标题 | 成功 | `睿眼自动化渗透系统` |
| 用户数据目录 | 成功 | 子进程参数为 `C:\Users\10519\AppData\Roaming\RenYan-Pentest` |
| 页面与启动品牌 | 成功 | 构建产物包含睿眼标题、图标、浅深色标志和启动资源 |
| 关于信息 | 成功，证据组合 | 早期动态界面显示客户端、引擎、构建与法律入口；最终产物静态确认免许可证类别 |
| 托盘五项操作 | 成功，缺少截图 | 单元测试验证五项顺序与回调，应用启动未报告托盘异常 |
| 安装程序 | 不适用 | 用户最终要求只构建企业版免许可证开发版本，不继续生成安装程序 |

## 六、历史构建与失败记录

- 根依赖阶段遇到损坏的 Yarn 缓存条目和 Electron 默认下载地址拒绝，精确处理对应缓存条目并使用版本不变的镜像后成功。
- 一次 Windows 诊断打包因临时目录文件占用返回 `EPERM`；核对目标后只清理 `release/win-unpacked.tmp`，重试成功。
- 用户调整验证目标时，正在执行的社区版生产构建被主动终止，随后改为普通企业版开发构建，最终再改为企业版免许可证开发构建。
- 早期 Windows 安装文件早于知识库品牌与免许可证类别修正，不代表最终源码，不列为交付产物。

## 七、截图状态

阶段一的启动页、主窗口、关于界面和托盘没有保存为仓库截图，仍按阶段一证据限制处理。阶段二的浅深工作台、展开菜单与关于对话框已完成动态检查，但本地桌面捕获接口未能稳定形成完整证据组。原任务允许在自动化不稳定时记录人工复验步骤，因此不提交二进制截图，也没有把缺失文件记为截图通过。

## 八、阶段二静态与单元验证

| 命令或检查 | 结果 | 证据摘要 |
| --- | --- | --- |
| `git branch --show-current` | 成功 | 输出 `qsh` |
| `git diff --check` | 成功 | 无输出 |
| `yarn i18n:check` | 成功 | 英文、简体中文和繁体中文键集合一致 |
| 阶段二定向测试 | 成功 | 五个测试文件、十九项用例通过，覆盖菜单、功能标志、首页指标、统一状态、浅深主题变量与产品主色映射 |
| `yarn ci:tsc` | 成功 | 第二次复验无类型错误，耗时约一百九十三秒 |
| `yarn ci:eslint` | 成功 | 零错误，一千六百九十九项既有警告，耗时约一百二十九秒 |
| `yarn test:vitest --run` | 成功 | 二十一个测试文件、二百二十一项用例通过，耗时约四十三秒 |
| `yarn ci:media-size` | 成功 | 没有需要提交的大体积媒体变更 |
| `yarn build-renders-test-enterprise-no-license` | 成功 | 企业版免许可证双渲染构建退出码为零，耗时约九百一十五秒 |
| `yarn build-render-test-enterprise-no-license` | 成功 | 产品主色修正后的主渲染构建退出码为零，耗时约八百四十秒 |

完整规则检查与类型检查曾尝试并行执行，因四分钟时限且存在资源争用而超时；该次结果未计入通过。最终验证中的第一次类型检查没有产生类型诊断，但命令宿主在结束时返回“找不到指定路径”；复查编译器、配置和目录均存在后，以相同命令串行复验并取得零退出码。

最终构建产物时间如下：

```text
app/renderer/pages/main/index.html                         2026-07-14 08:31:44
app/renderer/engine-link-startup/dist/index.html          2026-07-14 01:10:25
```

用户已明确只需查看企业版免许可证开发效果，因此阶段二采用企业版免许可证双渲染命令完成整体构建。后续产品主色修正只涉及主渲染源码，主渲染器单独重建成功；启动渲染器沿用同一源码状态下已经成功生成的产物。未执行社区版构建或安装程序生成。

## 九、阶段二桌面动态验证

桌面程序以 `ELECTRON_IS_DEV=0` 启动，使主进程读取最终静态渲染产物。已取得以下动态证据：

| 检查项 | 结果 | 动态证据 |
| --- | --- | --- |
| 应用启动与产品标题 | 成功 | 窗口显示“睿眼自动化渗透系统”，启动页、企业登录、本地项目列表和主工作区均可呈现 |
| 企业免许可证入口 | 成功 | 企业登录界面提供“跳过”，无需外部账号即可进入本地项目 |
| 工作台 | 成功 | 显示流量分析、安全测试、项目、流量、风险、引擎与团队状态；默认项目无流量和风险时显示真实空状态 |
| 一级菜单 | 成功 | 工作台、流量分析、安全测试、工具箱、项目与协作、结果中心、系统管理七组均可见 |
| 二级菜单 | 成功 | 流量分析四项、工具箱三项、结果中心两项和系统管理三项均按目标名称呈现 |
| 团队预览 | 成功 | 团队工作区与成员和角色显示“待团队阶段实现”，没有完成状态 |
| 隐藏菜单 | 成功 | 辅助文本断言确认国密算法、插件链路编排、报告导出和客户端测试总览均未出现；序号二十五无菜单 |
| 保留路由 | 成功 | 从流量分析菜单进入报文重放后，窗口标题更新为“报文重放 · 睿眼自动化渗透系统” |
| 关于入口 | 成功 | 系统管理中的关于入口打开产品对话框，显示客户端、引擎、构建版本和“企业版（免许可证）”类别 |
| 亮色主题 | 成功 | 工作台主色、选中态和快捷入口使用蓝色体系，成功状态使用青绿色 |
| 暗色主题 | 成功 | 工作台、菜单、空状态和底部状态区使用浅蓝主色与青绿色连接状态，正文与深色背景保持可读 |

Playwright 依赖未安装，且主进程明确禁止打包态 Chromium 远程调试开关，因此没有建立 Playwright Electron 会话。动态检查改用本地 Windows 桌面控制接口，通过辅助文本和窗口图像验证可见内容；该接口的文件捕获能力不稳定，因此不提交截图文件。

人工复验步骤如下：

1. 在仓库根目录设置 `ELECTRON_IS_DEV=0` 并执行 `yarn start-electron`，从企业入口选择“跳过”后进入本地项目。
2. 分别选择浅色和暗色主题，检查工作台标题、蓝色主色、青绿色连接状态、真实空状态、快捷入口及底部状态区。
3. 依次展开七个一级菜单，核对流量分析四项、安全测试三项、工具箱三项、项目与协作三项、结果中心两项和系统管理三项。
4. 确认团队工作区与成员角色显示“待团队阶段实现”且不可导航；确认国密算法、插件链路编排、报告导出、客户端测试总览和序号二十五菜单均不可见。
5. 从流量分析进入报文重放，确认窗口标题为“报文重放 · 睿眼自动化渗透系统”；从系统管理打开关于对话框，确认产品、客户端、引擎、构建及“企业版（免许可证）”类别。
6. 需要扩展视觉证据时，将浅色工作台、展开菜单和关于对话框分别保存到 `docs/renyan/evidence/stage2`，不得改写统计数据或界面状态。

## 十、阶段三引擎生命周期验证

验证时间：2026-07-14 13:36:11 +08:00。

| 命令或检查 | 结果 | 证据摘要 |
| --- | --- | --- |
| `git branch --show-current` | 成功 | 输出 `qsh` |
| 主侧脚本语法检查 | 成功 | 生命周期服务、更新处理器与打包钩子均通过 `node --check` |
| 启动渲染类型检查 | 成功 | 无类型错误 |
| 生命周期主侧定向测试 | 成功 | 三个文件、二十六项用例通过；真实 Windows 预置包校验与提取耗时约六十三秒 |
| 启动页定向测试 | 成功 | 四个文件、四十二项用例通过；覆盖十四种状态、本地、缺失、远程、界面动作与错误恢复 |
| `yarn test:vitest --run` | 成功 | 二十六个测试文件、二百四十九项用例通过 |
| `yarn ci:eslint` | 成功 | 零错误；一千六百九十九项既有警告 |
| `yarn ci:tsc` | 成功 | 无类型错误，耗时约二百四十五秒 |
| `yarn i18n:check` | 成功 | 三种语言的键集合一致 |
| `yarn ci:media-size` | 成功 | 没有提交体积超限的媒体变更 |
| `yarn build-renders` | 成功 | 社区版双渲染构建退出码为零，耗时约七百六十一秒 |
| `yarn pack-win` | 成功 | 当前 Windows 社区版安装构建退出码为零，耗时约四百零六秒；本地产物不提交 |

启动页专属测试首次执行时，面板测试因没有显式引入文档断言扩展而有两项失败；加入项目已有的测试断言扩展后，相同四文件命令复验为四十二项全部通过。远程看门狗测试仍输出一条既存的异步状态更新警告，退出码保持为零。

## 十一、阶段三启动冒烟

| 场景 | 自动化或动态结果 | 证据边界 |
| --- | --- | --- |
| 已安装且兼容，网络不可用 | 自动化通过，动态启动通过 | 本地测试明确断言启动流程不调用客户端版本、在线引擎版本和在线摘要接口；动态实例从本地文件加载，引擎 `1.4.8-beta3` 能力检查成功并进入工作台。未执行操作系统级断网切换，不将其记为系统断网动态通过 |
| 无本地引擎但有预置包 | 自动化通过 | 真实 Windows 压缩包及内部引擎双摘要验证、精确条目提取与原子安装通过；未迁移真实用户活动引擎进行破坏性动态复验 |
| 无本地和预置 | 自动化通过 | 决策函数进入 `missing`，界面只提供安装引擎、远程连接、手工安装和诊断入口，不触发自动下载；未改动真实用户引擎目录 |
| 下载失败 | 自动化通过 | 临时下载失败后已验证缓存与活动引擎保持不变 |
| 摘要不一致 | 自动化通过 | 安装被拒绝，活动引擎保持不变 |
| 解压失败 | 自动化通过 | 非法压缩包被拒绝，不替换活动引擎 |
| 新版本可用且旧版本兼容 | 自动化通过 | 旧版本继续启动，主界面后台状态显示可更新 |
| 不兼容版本 | 自动化通过 | 已知最低版本以下进入阻断状态；当前真实最低版本仍为 `TBD`，没有虚构数值 |
| 远程连接成功与失败 | 自动化通过 | 成功进入远程状态；失败显示恢复入口且不启动本地引擎。没有真实远程服务与证书环境，不记为真实远程动态通过 |
| 上次安装中断 | 自动化通过 | 安装标记与回退文件恢复测试通过 |
| 更新失败后旧版本仍可启动 | 自动化通过 | 下载、摘要和替换失败均保留原活动文件 |

## 十二、阶段三桌面动态证据

直接执行源码 Electron 时，`electron-is-dev` 将未打包应用判定为开发模式，两个窗口分别访问本地渲染开发地址；对应服务没有启动时可复现空白页。设置 `ELECTRON_IS_DEV=0` 后重启，启动页和主页面分别从 `app/renderer/engine-link-startup/dist/index.html` 与 `app/renderer/pages/main/index.html` 加载，首次设置页正常显示，随后窗口标题变为“工作台 · 睿眼自动化渗透系统”。

最新引擎日志记录 `check-secret-local-grpc` 返回成功，版本为 `1.4.8-beta3`，本地远程过程调用服务监听 `127.0.0.1:9011`，主窗口显示事件完成。后台版本信息查询在网络响应不完整时记录解析错误，但主界面保持可用，符合“未检查且不阻塞”的降级要求。

## 十三、未执行或不能标记为通过的场景

- macOS `x64`、macOS `arm64`、Linux `x64` 与 Linux `arm64` 没有真实预置工件、摘要和当前系统，未构建、未打包、未动态启动。
- 真实远程引擎、传输层安全证书错误、远程网络中断与跨版本远程组合没有可用服务环境，只有单元级成功与失败证据。
- 没有通过修改真实用户目录执行本地引擎缺失、预置替换、中断安装与失败替换的完整桌面流程；这些分支采用隔离临时目录与真实预置包验证。
- 没有执行操作系统级完全断网；离线启动由网络调用零次断言、本地能力检查与生产资源动态启动共同证明。
- 安装包由用户从最终提交重新生成；本次 `release` 目录仅用于本地验证，不是提交内容或发布工件。

## 十四、阶段九开发模式最终轻量验收

### 仓库与需求源

- 执行 `git fetch origin --prune`、`git switch qsh` 和 `git pull --ff-only origin qsh`，结果为分支已同步。
- `git branch --show-current` 返回 `qsh`，远端为 `https://github.com/sharptornadoqsh/yakit.git`，初始工作区无改动。
- 以只读方式重新打开 `D:\Desktop\Project\yakit围标\功能要求.xls`。摘要为
  `F16C9C671CD3D7580027F0069146C6649907306D479624B6A50AD0125DE7D05D`；有效工作表为 `Sheet1`，使用范围
  为三十三行、四列，另两个工作表为空。

### 页面查看

实际查看工作台、七个一级分组及其二级菜单、交互代理、历史流量、报文重放、报文差异分析、通用漏洞检测、
专项漏洞检测、爆破测试、编解码工具、插件中心、插件开发、项目与协作菜单、本地项目、本地风险、引擎与
更新、安全设置、关于、当前服务、用户管理及角色与权限。插件中心显示四十九个本地插件；本地项目页显示
一个项目；用户管理页从当前外部服务取得一个成员；角色与权限页取得三个角色。查看期间每秒请求数为零，
未提交账号、角色、密码、项目或服务地址变更。

关于界面显示睿眼品牌和产品名称；顶部黄色三角图标经组件来源复查确认为全局状态告警，不是旧品牌图标。
团队工作区和成员与角色不可导航；安全测试报告、国密算法、插件链路编排、纳管客户端总览未出现在默认
菜单中。私有域配置仅位于登录路径；为保留当前外部服务会话，本次没有主动退出并重新打开登录页。

### 导航修订与定向测试

修订前可复现：在工作台手动选择系统管理一级分组，再通过工作台历史流量快捷入口改变实际路由，顶部仍
显示系统管理，侧栏已显示流量分析。修订后以实际开发界面重复同一路径，窗口标题变为“历史流量 · 睿眼
自动化渗透系统”，顶部显示流量分析，侧栏同时选中历史流量。

执行以下定向命令：

```text
yarn test:vitest --run app/renderer/src/main/src/pages/layout/renyanMenu/__test__/RenyanNavigation.test.tsx app/renderer/src/main/src/routes/__test__/renyanMenu.test.ts
```

结果为两个测试文件、十项测试全部通过，退出码为零；仅出现项目既有的 `ReactDOM.render` 弃用警告。
两个目标代码文件的定向格式检查通过，且均为 UTF-8 无 BOM；十一份阶段九需求文档的相对链接检查结果为
零个缺失链接。

### 开发命令结果

- `yarn dev` 因本地 `node_modules/rxjs/internal/BehaviorSubject` 缺失而以退出码一终止，未修改依赖目录。
- `yarn start-render` 完成开发编译，显示仓库既有样式和规则警告，结尾为 `webpack compiled with 7 warnings`
  及 `No issues found.`。
- `yarn start-link-render` 在本地端口 `5173` 就绪。
- `yarn start-electron` 打开启动页并进入工作台；导航同步场景复验完成后，三个开发进程均以中断信号终止。

本阶段没有执行全量测试、全量类型检查、全量规则检查、生产构建、安装包生成、跨平台验证、长时负载、
真实多用户共享、低权限接口验证或第三方检测。

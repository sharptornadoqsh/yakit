# 前端品牌残留审计

审计时间：`2026-07-15 11:17:24 +08:00`

## 一、搜索范围与方法

审计限定在 `app/renderer/src/main/src`、`app/renderer/src/main/public`、主渲染入口、`app/renderer/engine-link-startup/src`、启动渲染入口、`app/main`、`product`、品牌直接相关配置以及本任务三份文档。依赖目录、构建产物、发布目录、缓存、锁文件、二进制内容、许可证正文与第三方依赖源码均未纳入字符串修改。

检索采用 CodeGraph 调用关系、`rg` 有界字符串筛选、类型脚本语法树字符串提取、三种语言资源值解析和删除资源直接引用检查。命中项按用户可见品牌、用户文件名称、启动资源、内部兼容标识、通信标识和法律归属逐项分类，没有进行全仓库替换。

## 二、已清理的旧品牌字符串

- 普通界面中的 `Yakit`、`YAKIT`、`yakit` 产品名称改为“睿眼自动化渗透系统”“睿眼”或 `RuiYan`。
- 用户可见的引擎名称改为 `RuiYan Engine`，远程过程调用服务显示名称改为 `RuiYan MCP`。
- 启动、下载引擎、更新、远程连接、插件、智能代理、在线判定、漏洞、历史会话、交互式终端、错误与成功提示中的旧产品身份已经修改。
- 三种语言资源采用相同品牌规则，国际化键名保持原状。

## 三、修改的页面与入口

启动渲染端涉及初始化页面、协议内容、问题反馈、远程引擎、引擎列表、引擎看门狗和桥接错误提示。初始化页面的右侧品牌装饰区已取消，正常启动时该窗口保持隐藏。

主渲染端涉及功能域、远程引擎、引擎更新、智能代理远程过程调用、系统远程过程调用配置、插件内置数据、在线判定、历史会话、指纹导入导出、交互式终端、漏洞列表、报告图形、编解码、截图、数据导出、流量导出、规则导出和证书下载。主侧涉及更新提示、窗口截图默认名称、反馈页面模板和桥接错误提示。

## 四、修改的下载与导出文件名

| 用途 | 当前默认名称 |
| --- | --- |
| 流量导出 | `RuiYan-HTTP-Flows-<时间戳>.har` |
| 替换规则 | `RuiYan-MITM-Replacer-Rules-Config.json` |
| 历史流量 | `RuiYan-History-<时间戳>` |
| 报文重放 | `RuiYan-WebFuzzer-<时间戳>` |
| 漏洞结果 | `RuiYan-Risks-<时间戳>` |
| 页面截图 | `RuiYan-Screenshot-<时间戳>.png` |
| 编解码输出 | `RuiYan-Codec-Output-<时间戳>.txt` |
| 报告图形 | `RuiYan-Diagram.png` |
| 通用数据导出 | `RuiYan` 前缀与原业务后缀 |

现有时间戳生成方式、过滤器、扩展名、序列化结构和保存桥保持原状。

## 五、修改的证书文件名

- 普通中间人证书：`RuiYan-MITM-CA.pem`
- 国密中间人证书：`RuiYan-MITM-GM-CA.pem`

证书真实内容仍由原接口取得，编码、私钥、安装、信任与校验流程没有变化。

## 六、删除的启动动画与旧品牌资源

初始化渲染端删除以下无引用资源：

- `YakitLogo.png`
- `yakit-right.png`
- `yakitEE.png`、`yakitEEDarkLogo.png`、`yakitEELogo.png`
- `yakitSE.png`、`yakitSEDarkLogo.png`、`yakitSELogo.png`
- `yakitSS.png`
- `renyan-startup-panel-dark.svg`、`renyan-startup-panel-light.svg`

主渲染端删除以下无引用资源：

- `aiAgent/yakit_run_quickly.gif`
- `uiLayout/YakitPrimaryBg.png`
- `yakit.jpg`、`yakitCattle.png`
- `yakitEE.png`、`yakitFontEE.png`
- `yakitFontSE.png`、`yakitLogo.png`、`yakitSE.png`

直接引用检查只在回归测试的资源不存在断言中发现这些文件名，没有发现业务引用。仍被其他产品类别或通用插图使用的文件继续保留。

## 七、保留的内部兼容标识

- `Hello Yakit!` 属于客户端与引擎的既有兼容握手内容，修改会影响连接判定。
- `Yakit` 版本类别与更新兼容值属于既有分支数据，修改会影响版本识别。
- `yakit-projects`、`YAKIT_HOME` 和既有用户目录片段属于固定路径或环境变量。
- 代码中的 `Yakit` 类型、组件、路由键、缓存键、事件名和导入路径属于内部符号。
- `Yaklang` 在脚本语言、代码审计语言、系统环境变量、运行器和桥接技术名称中表示技术能力，不作为当前产品品牌。
- `Yak` 引擎二进制、插件类型、远程过程调用、协议字段和数据库字段保持不变。

三种语言资源值解析得到三十个旧名称匹配：十八个来自固定目录 `yakit-projects`，十二个来自技术语言或环境变量 `Yaklang`。这些值均有明确兼容或技术语义。

## 八、保留的通信标识与法律归属

进程间通信通道、远程过程调用方法、接口路径、数据库字段、自动更新地址、引擎包名称、登录摘要、授权格式和外部服务协议没有修改。源代码兼容注释与模板内的技术标识按调用关系保留。

许可证正文、上游项目名称、原作者版权、第三方依赖名称、开源地址和第三方声明入口均保持原状。普通产品界面继续提供许可证和第三方软件声明入口，没有把上游著作权改写为睿眼归属。

## 九、未确认残留

有界审计中的候选项均已归入用户可见修改、固定路径、技术语言、内部符号、通信兼容或法律归属，没有遗留无法分类的候选项。未扩展到任务禁止的引擎源码、第三方依赖、构建产物、发布目录和许可证正文。

## 十、开发模式查看结果

主渲染端和初始化渲染端开发服务器均完成载入，桌面端启动后只有标题为“睿眼自动化渗透系统”的系统窗口可见。运行日志显示主窗口就绪时可见，初始化窗口就绪时不可见；两者均完成主框架加载，没有白屏或致命加载错误。

本任务动态检查覆盖直接进入系统界面、窗口标题和后台初始化窗口状态。前序阶段已经查看首页、一级与二级导航、服务登录、服务地址配置、交互代理、历史流量、项目、插件、关于和错误状态。用户中止桌面界面自动化后，本任务没有继续逐个触发保存对话框；证书、流量、项目、插件、日志与诊断入口的默认名称采用静态证据和定向测试验证。

# 睿眼产品化实现状态

## 阶段一：产品身份与构建适配

## 一、阶段范围

本阶段只处理产品身份、集中配置、打包配置、原创品牌资源、窗口、系统菜单、托盘、关于信息与独立用户目录。未修改协议、数据库、路由枚举、插件接口、引擎命名协议或团队协作功能。

## 二、交付状态

| 交付项 | 状态 | 主要证据 |
| --- | --- | --- |
| 单一产品配置 | 已完成并验证 | `product/renyan.json`、三个运行端适配、配置单元测试 |
| 应用名称与页面标题 | 已完成并动态验证 | 主侧身份初始化、两个页面入口、可见窗口标题 |
| 打包身份与平台元数据 | 已完成静态验证 | Electron Builder 配置、构建钩子、配置单元测试 |
| 独立用户数据目录 | 已完成并动态验证 | `app/main/product.js`、`filePath.js`、运行进程参数 |
| 系统菜单与托盘 | 已完成 | 菜单与托盘模块、五项操作单元测试、应用启动无托盘异常 |
| 关于信息与法律入口 | 已完成 | 主侧关于窗口、渲染端关于界面、版本类别构建注入、法律资源 |
| 原创品牌资源 | 已完成并自检 | `product/brand`、平台图标、两个渲染项目资源、生成脚本 |
| 企业版免许可证开发构建 | 已完成 | `yarn build-renders-test-enterprise-no-license`，退出码为零 |
| 本地应用启动 | 已完成 | 窗口标题为“睿眼自动化渗透系统”，进程响应正常 |
| 自动化验证 | 已完成 | 十六个测试文件、二百零二项用例、类型检查、多语言校验、规则检查 |
| 旧名称分类与阶段文档 | 已完成 | `docs/renyan` 七份阶段文档与 `.claude/verification-report.md` |

## 三、当前结论

阶段一源码实现与最终指定的企业版免许可证开发构建已经完成。应用使用睿眼专属用户目录，构建产物包含“企业版（免许可证）”类别值，窗口已启动供界面查看。

最终要求明确取消继续生成安装程序，因此当前有效开发产物为 `app/renderer/pages/main` 和 `app/renderer/engine-link-startup/dist`。macOS、Linux、社区版及其他产品变体未以最终源码状态构建。

## 四、证据限制

任务内动态检查显示了启动页、主窗口和关于界面，但截图未保存为仓库文件。自动化界面工具未提供系统通知区域，托盘菜单没有独立截图；托盘创建、五项菜单和回调由源码、单元测试与应用启动共同支撑。

## 阶段二：应用外壳与导航

### 一、阶段范围

阶段二仅改造主渲染端的菜单元数据、应用外壳、工作区布局、首页、主题变量、用户可见状态与对应多语言文本。既有路由值、页面协议、进程间通信、远程过程调用、数据库、引擎、代理、重放、扫描及插件执行逻辑均未改变。

### 二、实现状态

| 交付项 | 状态 | 主要证据 |
| --- | --- | --- |
| 配置化菜单模型 | 已实现并完成定向验证 | 九项规定字段、七个菜单组、能力过滤、功能标志及显示映射 |
| 一级与二三级导航 | 已实现 | 顶部产品导航、上下文菜单及三级子项呈现能力 |
| 可折叠工作区侧边栏 | 已实现 | 七组上下文切换、当前路由选择状态、展开与收起操作 |
| 页面标题与面包屑 | 已实现并完成动态验证 | 当前路由显示名称、产品面包屑、操作区及浏览器标题同步；内部路由键不作为可见标题 |
| 底部引擎状态区 | 已实现 | 引擎连接状态、本地或远程模式、团队服务交付状态 |
| 睿眼工作台 | 已实现 | 八个快捷入口、项目概览、流量概览、风险概览、引擎与团队状态 |
| 真实首页指标 | 已实现并完成定向验证 | `GetCurrentProjectEx`、`QueryHTTPFlows`、`QueryAvailableRiskLevel` |
| 统一状态组件 | 已实现并完成定向验证 | 空数据、读取中、错误、无权限、离线五类状态 |
| 新主题变量 | 已实现并完成自动验证 | 文档根节点统一蓝色主色、青绿色成功色及浅深色状态变量，页面与浮层共用 |
| 首期隐藏项 | 已实现并完成定向验证 | 序号十一、十五、十七和二十四默认隐藏；序号二十五无菜单 |
| 团队协作预览 | 待团队阶段实现 | 团队工作区、成员与角色及首页团队状态仅显示阶段标记 |
| 旧菜单与内部跳转 | 保留 | 旧菜单组件仍在源码中，现由睿眼外壳适配层选择；路由与底层页面未删除 |

### 三、当前验证状态

菜单、功能标志、首页指标、统一状态、主题变量及产品主色映射共十九项定向测试通过。全量二十一个测试文件、二百二十一项用例通过；类型检查、国际化键一致性、媒体体积检查、差异空白检查和完整规则检查通过，规则检查保留一千六百九十九项既有警告且无错误。企业版免许可证双渲染构建通过，产品主色修正后主渲染构建再次通过。Electron 已完成启动、首页、浅深主题、菜单、关于、隐藏项与报文重放路由动态检查；本地截图自动化不稳定，按任务允许方式记录人工复验步骤，详情见 `TEST_EVIDENCE.md`。

### 四、未交付边界

团队工作区和成员角色没有业务页面与服务实现，因此禁用导航并明确显示“待团队阶段实现”。报告导出、国密算法、插件链路编排和客户端测试总览通过默认关闭的功能标志隐藏；相关既有路由或底层能力没有删除，也没有被标记为阶段二完成。

## 阶段三：引擎生命周期与通用初始化

### 一、阶段范围

阶段三只处理引擎模式选择、本地安装判断、预置恢复、显式下载、摘要校验、活动文件替换、启动错误恢复、后台更新提示及初始化界面。远程过程调用名称、数据库字段、插件接口、代理、重放、扫描、企业类别与许可证判断均保持原状。

### 二、实现状态

| 交付项 | 状态 | 主要证据 |
| --- | --- | --- |
| 离线优先启动 | 已实现并完成定向验证 | 已安装引擎启动不请求客户端版本、在线版本或在线摘要 |
| 远程模式优先 | 已实现并完成定向验证 | 读取保存模式后直接进入远程连接，本地检查不执行 |
| 预置引擎恢复 | 已实现并完成实体工件验证 | 压缩包摘要、内部引擎摘要、精确条目提取与原子安装 |
| 缺失引擎引导 | 已实现并完成组件验证 | 显式安装、远程连接、手工安装与诊断日志入口 |
| 兼容清单 | 已实现 | 客户端、版本边界、平台、架构、工件、摘要、日期和状态 |
| 下载与安装事务 | 已实现并完成单元验证 | 临时下载、摘要校验、版本缓存替换、活动引擎替换、最后可用副本 |
| 中断恢复 | 已实现并完成单元验证 | 安装标记、回退文件与最后可用副本的启动前恢复 |
| 后台更新提示 | 已实现并完成单元验证 | 主界面本地引擎已连接后查询；离线降级为“未检查” |
| 通用初始化界面 | 已实现 | 睿眼浅深色资源、真实步骤、进度、错误、动作和日志入口 |
| 自动化验证 | 定向验证已完成 | 生命周期、下载、摘要、预置、远程、本地和界面测试 |
| 完整构建与打包 | 已完成本地验证 | `yarn build-renders` 与 `yarn pack-win` 均以零退出码完成；用户要求只提交源码，产物不纳入提交 |

### 三、平台状态

Windows `x64` 预置压缩包与内部引擎已经取得真实摘要。macOS 与 Linux 四种目标没有实体压缩包，清单中的摘要和验证日期保持 `TBD`；构建钩子会拒绝这些目标的发布打包，不能将其描述为已验证。

社区版生产资源模式已完成动态检查。启动页和主界面均从本地构建目录加载，本地引擎 `1.4.8-beta3` 通过能力检查并进入工作台。源码模式若未同时启动两个渲染开发服务，会按开发地址加载并显示空白；这不是生产构建产物故障，复验命令必须显式使用生产资源模式。

### 四、原有能力保留

引擎启动仍使用原远程过程调用与进程管理接口。在线下载地址与摘要地址继续由既有上游规则生成，没有替换为其他服务。下载校验被设为安装前置条件，企业类别和许可证分支没有改变。

## 阶段四：安全功能页面与导航整理

### 一、阶段范围

阶段四只整理现有代理、流量、重放、扫描、爆破、差异对比、编解码、插件与项目管理能力的前端入口、标题、说明、状态呈现和基础布局。远程过程调用名称、进程间通信通道、数据库字段、引擎协议、企业类别与许可证判断均保持原状。

### 二、实现状态

| 交付项 | 状态 | 主要证据 |
| --- | --- | --- |
| 用户可见名称 | 已完成并定向验证 | 交互代理、报文重放、历史流量、漏洞检测、爆破测试、编解码工具、报文差异对比 |
| 页面标题与说明 | 已完成并动态查看 | 全局页面标题组件读取路由说明，目标页面均显示产品路径、功能标题与说明 |
| 通用状态呈现 | 已完成并定向验证 | 路由读取和错误状态、爆破类型列表、编解码方法列表均复用统一状态组件 |
| 项目管理说明 | 已完成并动态查看 | 本地项目、加密存储及明文或加密导入导出说明 |
| 工作台快捷入口 | 已完成静态修订 | 三个语言目录的首页文案与菜单名称一致 |
| 首期隐藏功能 | 已复查 | 报告导出、完整国密算法工具、插件链路编排、服务端安全总览继续隐藏；序号二十五无菜单节点 |
| 原有功能模块 | 保留 | 代理、重放、扫描、爆破、插件与项目管理底层代码均未删除 |

### 三、开发态查看

用户手动生成企业版免许可证开发工具界面并启动应用。阶段四在本地引擎和企业登录态下实际查看项目管理、交互代理、历史流量、报文重放、漏洞检测、专项漏洞检测、爆破测试、报文差异对比、编解码工具、插件中心和插件开发页面。每个页面均检查一个焦点交互，并检查标题、说明、主要内容、状态区域与页面横向溢出；未捕获页面级脚本异常或致命错误状态。

菜单逐组读取后没有发现本期隐藏功能。工作台快捷入口最终名称在动态查看后修订，因此该项只有源码和资源文件验证，没有再次生成界面。

### 四、验证边界

菜单与统一状态组件定向测试通过，共两份测试文件、十一项用例。三个语言目录的相关资源文件通过结构解析，差异空白检查通过。没有执行真实代理会话、请求转发、扫描、爆破、插件执行、项目导入导出、全量类型检查、全量测试、安装包生成、跨平台检查或压力测试。

详细页面清单、未验证功能与已知问题见 `CURRENT_HANDOFF.md`。

## 阶段五：协作能力快速审计

### 一、阶段范围

阶段五只复验需求序号 19、20、21、24、28、29、30，调查账号、角色、权限、项目层级、企业数据上传、密码复杂度与密码保存方式。没有开发服务、修改业务页面、调整企业类别、改动许可证判断、执行构建或软件打包。

### 二、审计结论

| 序号 | 当前状态 | 关键结论 |
| ---: | --- | --- |
| 19 | 部分存在 | 存在企业漏洞与 HTTP Flow 上传链路，没有多客户端共享读取证据 |
| 20 | 只有前端页面，没有可用后端 | 角色页面调用外部 HTTP 接口，仓库内没有角色业务路由，权限范围仅覆盖插件类型与标识 |
| 21 | 部分存在 | 项目与两级文件夹 RPC 可用，服务端项目组和多用户关系没有证据 |
| 24 | 不存在 | 只有数据上传与默认关闭的菜单项，没有服务端受管客户端测试记录总览 |
| 28 | 只有前端页面，没有可用后端 | 账号、组织和角色分配页面存在，服务端授权实现不在仓库内 |
| 29 | 部分存在 | 变更密码表单具有复杂度正则，初始与重置密码策略依赖外部服务且无法确认 |
| 30 | 不存在 | 私有域密码以可逆 Base64 保存，远端数据库存储方式也无法确认 |

### 三、复用决定

现有账号页、角色页、项目数据类型和统一状态组件可以作为阶段六的前端基础，但当前没有满足本阶段约束的可用业务后端。阶段二“团队工作区”和“成员与角色”继续保持计划状态且不可导航，不接入旧账号与角色页面。

序号 19、20、21、28、29、30 需要阶段六处理服务契约与本地验证；序号 24 按本期允许不交付的范围继续关闭。完整字段、接口和风险见 `COLLAB_CAPABILITY_AUDIT.md`，交接边界见 `CURRENT_HANDOFF.md`。

### 四、验证边界

本阶段只执行需求表只读提取、限定源码证据检查、文档检查、分支检查与差异空白检查。没有调用企业外部服务，没有启动开发模式，没有执行全量类型检查、全量测试、浏览器自动化、构建或打包，因此账号页、角色页和未配置服务状态没有动态通过结论。

## 阶段七：外部账号服务与权限集成

### 一、阶段范围

阶段七复用仓库外已部署的账号服务，验证服务地址配置、登录、当前用户、用户管理、角色与权限、会话退出及认证失效分支。仓库内没有新增服务端模块、模拟数据或团队协作功能。

阶段五结论只说明本仓库缺少服务端实现。本阶段的动态证据表明，既有客户端可使用外部服务完成账号与角色管理基础链路。

### 二、实现状态

| 交付项 | 状态 | 主要证据 |
| --- | --- | --- |
| 服务地址配置 | 已修订并完成静态验证 | 根地址统一生成单一 `/api/` 基址；地址变化时清空原会话 |
| 标准响应解析 | 已修订并完成动态验证 | `{code,message,data}` 由主侧 HTTP 层解析，登录令牌与角色可被客户端读取 |
| 服务登录 | 已完成动态验证 | 已启动服务返回管理员身份，主侧用户标识与令牌存在 |
| 用户管理 | 列表已完成动态验证 | 组织结构、账号记录与角色展示可见；未改动服务数据 |
| 角色与权限 | 列表已完成动态验证 | 三类内置角色可见；权限列读取服务字段 |
| 页面权限门槛 | 已完成静态验证 | 未登录或非管理员显示统一无权限状态 |
| 角色保存数据 | 已修订并完成静态验证 | 插件详情使用标识值，插件类型与插件标识分别序列化 |
| 会话退出 | 已修订并完成静态验证 | 远端退出失败、配置缺失或读取失败时，本地用户状态仍会清空 |
| 用户可见名称 | 已完成动态验证 | 服务登录、服务地址配置、用户管理、角色与权限、当前服务、退出服务登录 |
| 新服务端 | 未创建 | 客户端复用外部服务契约 |

### 三、开发态验证

用户已启动外部账号服务。开发态客户端完成服务登录，管理员菜单显示“用户管理”和“角色与权限”。用户管理页面读取到组织结构及一条账号记录；角色与权限页面读取到三条内置角色记录。当前服务没有为这些角色返回插件操作权限，权限列显示 `-`。

动态查看没有提交创建、编辑、删除、密码重置或服务地址变化操作，也没有保存真实密码、完整令牌或账号详情到仓库。

### 四、验证边界

当前仅有管理员账号，多角色权限差异与无权限页面尚未动态验证。离线、错误密码、令牌过期和服务地址变化没有主动制造，相关结论来自源码。密码在登录请求前生成 MD5；本地配置采用可逆 Base64 编码，服务端存储方式未知。

本阶段只做基础页面查看与限定源码审阅，没有启动全量测试、全量类型检查、全量规则检查、构建、打包、跨平台验证或性能测试。详细契约与限制见 `EXTERNAL_ACCOUNT_SERVICE.md`，当前移交材料见 `CURRENT_HANDOFF.md`。

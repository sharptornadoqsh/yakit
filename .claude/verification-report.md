# 启动、单文件产物与引擎架构独立验证报告

审查时间：`2026-07-15 15:48:46 +08:00`

## 一、需求与交付审查

| 审查项 | 结果 |
| --- | --- |
| 安装归档包含主进程所需构建元数据模块 | 通过 |
| 源码生产模式资源路径以仓库根目录为基准 | 通过 |
| 主渲染器与初始化渲染器生产入口存在 | 通过 |
| 每个平台和架构只上传一个安装文件 | 通过 |
| 前端英文短名称与可见字标使用 `RuiYan` | 通过 |
| 内部应用标识、可执行文件名和数据目录保持兼容 | 通过 |
| 苹果臂架构采用对应引擎工件 | 通过 |
| 开源系统英特尔架构采用对应引擎工件 | 通过 |
| 托管安装文件包含固定版本浏览器扩展 | 通过 |
| 普通企业类别行为保持不变 | 通过 |
| 免许可证企业类别保持独立输入与文件名 | 通过 |
| 代码、配置、测试、文档与审计材料映射明确 | 通过 |
| 审查结论包含时间戳、评分、风险和建议 | 通过 |

## 二、关键证据

- 企业版双渲染器生产构建以零退出码完成，两个入口文件均存在；输出中旧短名称匹配数量为零，新短名称存在于四个输出文件。
- 微软系统企业版无签名安装构建以零退出码完成，安装文件大小为 `119560289` 字节。
- `app.asar` 包含 `app/main/about.js` 与 `product/build.js`；一百一十六个主进程文件的相对依赖扫描没有缺失项。
- 源码生产模式动态路径检查把 `bins/resources` 解析到仓库内部，不再定位到仓库上两级目录。
- 任务 `29396021957` 的四个平台均记录 `INCLUDE_ENGINE=true`；该历史任务确实启用了预置引擎。
- 同一任务中，苹果臂架构与开源系统英特尔架构的准备步骤正确，打包钩子随后选择了相反工件；固定任务架构声明直接覆盖该故障边界。
- 第二段远端失败日志记录 `sign_installers=false`，因此不属于签名故障。
- 浏览器扩展压缩包未受版本控制；工作流现在下载固定 `0.0.7` 文件，并校验固定 SHA256 后才进入渲染与打包。

## 三、本地验证记录

- 定向测试两个文件、十八项用例通过。
- 工作流 YAML 通过项目测试解析，四个任务的固定架构、直接单文件上传、默认布尔输入和类别映射均有断言。
- 官方 `actions/upload-artifact@v7` 动作定义确认 `archive: false` 只接受单个文件，并使用文件名作为产物名。
- 本地浏览器扩展文件的 SHA256 与工作流固定摘要一致。
- 主进程路径文件与打包配置通过语法检查，产品配置与网页清单通过 JSON 解析，品牌生成脚本通过 Python 编译检查。
- Prettier、差异空白与 CodeGraph 同步检查通过；三份锁文件未修改。
- 未在微软系统主机生成苹果磁盘映像或开源系统应用映像，未主动触发远端工作流。
- 解包程序的隐藏窗口短时启动提前退出且没有生成错误日志，因此未把该项计为界面级动态通过；归档依赖完整性与用户原始模块故障已由归档扫描覆盖。

## 四、技术评分

| 维度 | 分数 | 依据 |
| --- | ---: | --- |
| 代码质量 | 96 | 修复限定在资源路径、打包清单、架构边界、上传配置与显示资源，没有改动引擎协议 |
| 测试覆盖 | 93 | 十八项定向用例、生产渲染构建、微软安装构建与归档扫描通过；苹果和开源系统仍需托管环境验证 |
| 规范遵循 | 97 | 分支、格式、编码、日志、文档、索引与禁止自动发布约束均有证据 |

## 五、战略评分

| 维度 | 分数 | 依据 |
| --- | ---: | --- |
| 需求匹配 | 97 | 启动模块、空白页、单文件下载、前端名称与两类架构故障均已覆盖 |
| 架构一致 | 96 | 复用现有工作流、兼容清单、元数据程序和打包钩子，只增加固定任务架构声明 |
| 风险评估 | 94 | 历史任务输入、未执行平台、隐藏窗口验证限制和远端复验要求均已记录 |

## 六、综合结论

综合评分：96 分。

建议：通过。

本地证据满足提交条件。新提交仍需由用户从 `qsh` 手动创建一次定向任务，分别验证未预置引擎路径和启用预置引擎路径；不得复用历史任务的网页表单状态判断历史输入。

## Windows 启动空白与标题栏专项复核

复核时间：2026-07-15 17:48:20 +08:00。

### 需求与交付映射

- 启动后不再持续空白：主窗口等待启动窗口完成工作区确认与引擎握手后显示；桌面检查观察到启动状态界面和非空白企业界面。
- Windows 窗口按钮位置正确：主布局挂载时独立读取系统类型；桌面检查观察到最小化、最大化和关闭按钮位于右上角。
- GitHub 工作流安装包包含修复：企业免许可证双渲染生产构建与 Windows 无签名打包命令均以零退出码完成，新安装文件已经生成；源码将在 `qsh` 提交并推送后供后续任务使用。
- 回归保护：产品测试先失败后通过，覆盖启动窗口显示、主窗口等待、完成事件、工作区组件与平台初始化。

### 本地验证证据

- 产品定向测试：九项用例通过。
- 类型检查：零退出码。
- 企业免许可证双渲染生产构建：零退出码。
- Windows 企业免许可证安装构建：零退出码。
- 主进程语法、目标文件格式与差异空白：全部通过。
- Windows 桌面检查：启动界面、非空白企业界面和右上角窗口按钮通过；用户现场确认可以启动。

### 技术评分

| 维度 | 分数 | 依据 |
| --- | ---: | --- |
| 代码质量 | 98 | 仅迁移一个窗口显示动作并复用现有平台查询函数，没有改变引擎与许可证协议 |
| 测试覆盖 | 96 | 包含失败复现、定向回归、类型检查、生产构建、安装构建与桌面检查 |
| 规范遵循 | 98 | 分支、格式、编码、记录、既有工具复用与文件范围均符合仓库约定 |

### 战略评分

| 维度 | 分数 | 依据 |
| --- | ---: | --- |
| 需求匹配 | 98 | 同时覆盖持续空白、Windows 标题栏位置和后续工作流安装文件来源 |
| 架构一致 | 98 | 恢复既有双窗口完成事件协议，平台查询沿用已有硬件常量模块 |
| 风险评估 | 95 | 记录了构建既有警告、工作区与引擎前置条件以及远端任务必须使用新提交 |

### 综合结论

综合评分：97 分。

建议：通过。

# 启动提示、品牌与微软系统安装文件最终复核

复核时间：2026-07-15 22:27:53 +08:00。

本节取代前文关于“工作空间与引擎状态启动界面”的旧描述，作为当前提交的最终结论。

## 需求与交付映射

| 审查项 | 结果 | 证据 |
| --- | --- | --- |
| 启动过程只显示统一提示 | 通过 | 桌面截图仅显示品牌、产品英文名、中文启动文案和动画 |
| 旧工作空间与引擎状态界面不再显示 | 通过 | 启动页移除可见表单、生命周期、终端和诊断组件，组件测试排除对应文案 |
| 启动控制流程保持有效 | 通过 | 本地、远程、连接监视与下载组件继续在不可见控制层挂载 |
| 产品英文名统一为 `RuiYan-Pentest` | 通过 | 产品配置、清单、英文资源、品牌字标与工作台可见内容一致 |
| 初次启动的数据目录有效 | 通过 | 主进程在设置用户数据路径前创建 `RuiYan-Pentest` 目录，回归测试覆盖调用次序 |
| 微软系统窗口按钮位于右上角 | 通过 | 平台状态独立初始化，企业工作台桌面查看确认位置 |
| 企业服务可登录并进入工作台 | 通过 | 本机服务响应正常，提交用户提供的凭据后显示企业工作台；凭据未持久化到仓库 |
| 安装归档可解析并可启动 | 通过 | 排除动态本地索引目录后，归档元数据可读，解包应用启动后进程持续存在 |
| 六十四位安装文件生成 | 通过 | 无签名打包命令完成并生成单一可执行安装文件 |

## 本地验证证据

- 四个定向测试文件共二十一项用例通过；最终产品与打包定向测试共十八项用例再次通过。
- 启动渲染器类型检查、根项目类型检查、主进程语法与打包配置语法通过。
- 企业免许可证双渲染器生产构建与微软系统六十四位安装构建通过。
- 国际化、媒体体积、格式、差异空白和敏感信息扫描通过；规则检查为零错误，现有一千六百九十四项警告未由本次变更引入。
- 安装文件大小为 `162834643` 字节，SHA256 为 `DDBA4AA672CA55B8ECBC4829B423B606EDA3D907A9BE81C5230C0FF222255B94`。
- 最终归档名称为 `ruiyan-pentest`，不含 `.codegraph` 与 `.claude`；解包应用启动五秒后存在三个匹配进程。
- 桌面验证覆盖单一启动提示、企业登录、非空工作台、精确英文名称和微软系统窗口按钮。
- 提交前启动页完整相邻测试共七个文件、五十五项用例通过；一项既有 React 测试状态更新警告不影响退出码与断言结果。

## 技术评分

| 维度 | 分数 | 依据 |
| --- | ---: | --- |
| 代码质量 | 97 | 显示层与控制层职责明确，沿用现有引擎协议和窗口事件，归档排除项限定于开发资料 |
| 测试覆盖 | 97 | 覆盖组件文案、启动契约、目录创建、窗口交接、产品名称、归档清单、构建与桌面行为 |
| 规范遵循 | 98 | 分支、格式、编码、中文记录、相邻测试、本地验证和敏感信息处理均有证据 |

## 战略评分

| 维度 | 分数 | 依据 |
| --- | ---: | --- |
| 需求匹配 | 98 | 单一启动提示、技术内容隐藏、精确品牌、企业登录、窗口按钮和可执行安装文件均已覆盖 |
| 架构一致 | 96 | 可见层简化后继续沿用启动控制链路，主窗口协议、主题、产品配置与平台查询保持单向依赖 |
| 风险评估 | 96 | 已验证初次目录创建、归档偏移、进程占用、远程配置失效与敏感凭据边界 |

## 范围与风险

- 本次本地安装构建限定于微软系统六十四位企业免许可证类别；苹果系统与开源系统未在当前主机生成安装文件。
- 启动流程不再展示技术日志；连接异常仍由既有主界面引擎管理入口处理，内部协议标识未进行破坏性重命名。
- 规则检查中的警告属于仓库既有基线，本次结果为零错误。

## 综合结论

综合评分：97 分。

建议：通过。

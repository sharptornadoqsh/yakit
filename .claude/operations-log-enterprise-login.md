# 企业登录界面操作记录

## 一、编码前检查

- 时间：2026-07-14 11:31:09 +08:00。
- 分支检查输出 `qsh`；工作区已有阶段三记录和引擎生命周期测试，本任务不修改这些文件。
- 已查阅 `.claude/context-summary-enterprise-login.md`。
- 复用 `ConfigPrivateDomain`、`YakitAutoComplete`、`YakitInput`、`YakitButton`、`WinUIOp`、`MacUIOp`、产品配置与原创图标。
- 遵循组件大驼峰、函数与变量小驼峰、相邻样式、两空格、单引号、无分号和尾逗号约定。
- 已对比共享私有域表单、企业门禁页、窗口控制、旧登录页和许可证页，没有发现现成的登录专用应用外壳。
- 变更限定在呈现层；登录、许可证、网络、配置、同步、引擎和通信协议保持原状。

## 二、实现记录

- 时间：2026-07-14 13:25:50 +08:00。
- `EnterpriseJudgeLogin` 改为企业访问门户双栏布局，品牌区使用现有产品配置和睿眼图标，表单区复用原认证组件。
- `ConfigPrivateDomain` 增加显式页面模式；默认弹窗模式保持原样，页面模式使用纵向标签、完整宽度登录按钮和低层级本地入口。
- `UILayout` 在企业登录门禁显示期间隐藏业务工具栏和底部状态栏，仅保留产品标题及平台窗口控制。
- 英文、简体中文和繁体中文资源增加企业登录文案，三种语言的键保持一致。
- 新增企业登录页面组件测试，覆盖品牌信息、页面模式参数和认证成功后的门禁退出回调。
- 未增加第三方依赖、图片、生成包或打包产物；未修改认证请求、许可证逻辑和引擎协议。

## 三、验证记录

- `yarn test:vitest --run app/renderer/src/main/src/pages/__test__/EnterpriseJudgeLogin.test.tsx`：两个测试通过。
- 首次组件测试因本机缺少 `sass-embedded` 未进入断言；测试随后隔离样式模块，未增加依赖，复测通过。
- `yarn i18n:check`：英文、简体中文和繁体中文键一致。
- `yarn ci:tsc`：通过。
- 相关文件定向代码规范检查：零错误；二十条警告均位于既有大文件规则范围内。
- `yarn build-render-test-enterprise-no-license`：企业免授权主渲染器编译成功，产物包含企业版、开发工具和免授权构建常量。
- 首次桌面启动遇到构建目录并发覆盖，日志记录 `ERR_FILE_NOT_FOUND`；结束并发构建后重新编译，空白窗口不再出现。
- 用户提供的最终暗色主题截图显示双栏企业登录页、三个必填字段、密码可见性、登录按钮和最小化、最大化、关闭按钮；业务工具按钮及底部状态栏未显示。
- 电脑控制在最终截图读取前由用户停止；最终可见结果由用户截图完成确认。

## 四、编码后声明

- 复用了 `ConfigPrivateDomain`、`WinUIOp`、`MacUIOp`、主题变量、产品配置、现有睿眼图标和既有认证状态机。
- 新增代码遵循现有组件、函数、样式模块和多语言文件组织方式。
- `ConfigPrivateDomain` 的页面模式必须显式开启，现有设置弹窗不受影响。
- `UILayout.tsx` 同时存在另一任务的引擎更新改动，提交时只暂存企业登录门禁可见性相关差异。
